let { registro } = require('./src/index.js')

module.exports = require('./src/index.js');

module.exports.getSarcastic = (args) => {
  return args.split("")
  .map((char, i) => char[`to${i % 2 ? 'Upper' : 'Lower'}Case`]()).join('');
};
